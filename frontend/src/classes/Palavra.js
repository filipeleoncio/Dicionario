export default class Palavra {
    nome;
    tonicidade;
    isCanonica;

    constructor ( nome, tonicidade, isCanonica ) {
        this.nome = nome;
        this.tonicidade = tonicidade;
        this.isCanonica = isCanonica;
    }
}
